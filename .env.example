# Copy to .env and fill in your values
# PostgreSQL connection — matches db-delay-pipeline docker-compose defaults
DB_HOST=localhost
DB_PORT=5432
DB_NAME=db_delays
DB_USER=pipeline_user
DB_PASSWORD=pipeline_pass
